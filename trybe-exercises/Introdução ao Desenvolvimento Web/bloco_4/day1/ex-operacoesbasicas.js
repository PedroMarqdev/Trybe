let x = 5;
let y = 10;

// Soma
function soma(x, y) {
    return x + y;
}

// console.log('A soma dessas variáveis é + $[soma()]';

// Subtração

function subtracao(x, y) {
    return x - y;
}

// console.log("A subtração dessas variáveis é " + subtracao);

// Multiplicação

function multiplicacao(x, y){
    return x * y;
}

// console.log("A subtração dessas variáveis é " + multiplicacao);

// Divisão

function divisao(x, y){
    return x / y;
}
// console.log("A divisão dessas variáveis é " + divisao);

// Módulo

function modulo(x, y) {
    return x % y;
}
// console.log("O módulo dessas variáveis é " + modulo);